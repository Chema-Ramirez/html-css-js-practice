let speed = 135;
const limit = 60;

if(speed > limit){
    console.log(`You have to slow down. You're going ${speed} km/h. And the speed limit is ${limit} km/h.`)
}else{
    console.log("You can continue at that speed")
}